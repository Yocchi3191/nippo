{
	"name": "Nippo",
	"dockerComposeFile": "../docker-compose.yml",
	"service": "nippo",
	"workspaceFolder": "/nippo",
	"mounts": [
		"source=${localEnv:HOME}/.claude,target=/root/.claude,type=bind,consistency=cached",
		"source=${localEnv:HOME}/.ssh,target=/tmp/wsl-ssh,type=bind,readonly,consistency=cached"
	],
	"postCreateCommand": "cp -r /tmp/wsl-ssh/. /root/.ssh && chown -R root:root /root/.ssh && chmod 700 /root/.ssh && chmod 600 /root/.ssh/* && git config --global --add safe.directory /nippo && npm install -g @anthropic-ai/claude-code && cd /nippo/backend && npm install",
	"customizations": {
		"vscode": {
			"extensions": [
				"esbenp.prettier-vscode",
				"humao.rest-client",
				"Anthropic.claude-code",
				"ms-azuretools.vscode-containers",
				"rphlmr.vscode-drizzle-orm",
				"dbaeumer.vscode-eslint",
				"Hono.hono",
				"alexcvzz.vscode-sqlite"
			]
		}
	}
}