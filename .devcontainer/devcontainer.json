{
	"name": "Node.js & TypeScript",
	"dockerComposeFile": "docker-compose.yml",
	"service": "app",
	"workspaceFolder": "/app",
	"mounts": [
		"source=${localEnv:HOME}/.claude,target=/root/.claude,type=bind,consistency=cached",
		"source=${localEnv:HOME}/.ssh,target=/tmp/wsl-ssh,type=bind,readonly,consistency=cached"
	],
	"postCreateCommand": "cp -r /tmp/wsl-ssh/. /root/.ssh && chown -R root:root /root/.ssh && chmod 700 /root/.ssh && chmod 600 /root/.ssh/* && npm install -g @anthropic-ai/claude-code && npm install",
	"customizations": {
		"vscode": {
			"extensions": [
				"esbenp.prettier-vscode",
				"humao.rest-client",
				"Anthropic.claude-code"
			]
		}
	}
}